# Copy to your user Go config (run once):
#   go env -w GOTOOLCHAIN=local
#
# Or set before each command:
#   export GOTOOLCHAIN=local   # Git Bash
#   $env:GOTOOLCHAIN="local"   # PowerShell
#
# This stops Go from trying to download toolchains that fail on some Windows setups.

GOTOOLCHAIN=local
